---
layer: knowledge
type: spec
last_verified: 2026-06-07
teaches: "fx-ui 是什么产品、给谁用、解决什么问题、设计调性是什么"
use_when: "需要判断一个新功能/组件是否符合 fx-ui 的产品定位时"
---

# 产品定义 — fx-ui

> 用途：回答"这个产品是什么、给谁用、什么调性、设计边界在哪"。
> 跟 `PROJECT.md` 的分工：这里讲"为什么存在、长什么样"，`PROJECT.md` 讲"做到哪一步了"。

## 产品定位

- 产品名：`fx-ui`
- 一句话定位：公司的 **AI 可读前端生产体系**——基础组件全用现成 shadcn/ui，公司视觉靠 token 注入，业务页面从 Blocks 起步并沉淀成布局规范
- 产品形态：内部开发者工具 / 组件文档站（不是面向最终用户的产品）
- 不是什么：不是又一个手写的 UI 组件库；不是公司老 Element 库的换皮重写

## 使用者

fx-ui 的"用户"分两类，文档和组件设计都要同时照顾到：

- **人类工程师**：需要能直接抄 import / JSX 调用、能看懂 props 和约束、能在源码里改样式
- **AI（Claude / Copilot 等）**：需要结构化、可解析的语义信息（变体、尺寸、约束、语义 DOM），照着写代码不会写错

## 解决的问题

- 老 Element 改造库是黑盒，AI 读不懂、改不动，工程师也经常误用样式
- 公司视觉散落各处，每个项目自己定一套颜色/圆角/间距，缺统一标准
- 业务页面经常从零手写，重复造轮子，也容易跑偏视觉规范

fx-ui 的解法：组件用 open-code 的 shadcn/ui（源码可读可改），公司视觉统一收敛进 token（`theme/fx-theme.css`），业务页面从现成 Blocks 起步再沉淀公司自己的组合组件和布局规范。

## 设计原则

1. **不手写组件**：一律 `npx shadcn add` 拉现成的，组件正确性交给 shadcn 维护
2. **只注入 token，不重新设计**：公司视觉变化只通过改 `theme/fx-theme.css` 里的语义变量实现，不在组件层面动手脚
3. **open-code，不做黑盒封装**：组件源码进 `src/components/ui/`，可见可改，AI 和工程师都能直接读懂
4. **从真实页面沉淀规范**：先有页面 demo，再从中抽公司组合组件（`src/components/fx/`）和布局规范（`docs/LAYOUTS.md`），不先验式设计

## 调性与视觉基调

- 品牌色、圆角、阴影、焦点环等具体 token 值见 `docs/TOKENS.md`，本文件不重复列举
- 当前已验证：公司橙（`#FF8000`）注入 shadcn 语义槽后换肤成功
- 调性关键词：克制、专业、AI 友好（结构化优先于花哨视觉）

## 边界（fx-ui 不做什么）

- 不做账号体系、权限系统等业务后端能力——它是纯前端的组件 + 文档体系
- 不维护、不兼容老 Element 库（暂不扫描，需求清单二期再议）
- 不追求"大而全"的组件覆盖，优先覆盖公司业务页面真实出现过的模式
