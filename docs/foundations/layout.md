---
layer: knowledge
type: spec
last_verified: 2026-08-28
teaches: "FDS Web 页面骨架、容器尺寸与内容节奏规则"
use_when: "设计或实现 Header/Sider/Content/Footer 页面骨架时"
---

# FDS 布局规范

> 本文是 FDS Foundation 专题说明；数值与映射的唯一真相源仍是 `tokens/source/*.tokens.json`。总览见 [设计 Token](../TOKENS.md)，统一目录见 [FDS 文档索引](../INDEX.md)。

## 页面布局容器（6 种样式）

来源：企业 Web 布局规范。按页面复杂度从简到繁：

| 样式 | 结构 | 场景 |
|------|------|------|
| 一 | Header + Main | 最常见基础页 |
| 二 | Header + Main + Footer | 带固定底部 |
| 三 | Header +（左侧 Menu ｜ Main）+ Footer | 二级左侧导航 |
| 四 | Header + 顶部 Menu + Main + Footer | 二级顶部导航 |
| 五 | 三栏：左右功能区 + 中间画布操作区 | 编辑器/画布类 |
| 六 | 左侧一级导航 +（Header + Main + Footer） | **新版趋势**，推荐 |

> 样式六（左侧一级导航）是新版趋势，新建后台优先考虑；`PageShell` 应能承载这些容器骨架。

### 容器尺寸（默认值）

框架（头/侧/内容/底）用 **flex** 拼；**内容区内部**才用 24 列栅格——两者分工：flex 管整页骨架，栅格管内容分栏。参考 Semi / Ant，钉一套默认尺寸：

| 部位 | 默认值 | 说明 |
|------|--------|------|
| 顶栏 Header | 高 **56px** | 紧凑工具栏，不做大面积品牌展示 |
| 侧栏 Sider | 展开 **240px** / 收起 **64px** | 收起为图标栏（仅图标 + tooltip） |
| 底栏 Footer | 高 **48px** | 可选，多数后台页不需要 |
| 内容区 Content 内边距 | 移动 **16px** / 桌面 **24px** | 对应 `px-4` → `lg:px-6` |

- **侧栏响应式**：视口 `< lg(1024px)` 时侧栏自动收起为 64px 图标栏（或转抽屉），避免挤压内容。
- 数值是默认基线，具体业务可在此基础上调整，但不要逐页随手定。

## 间距规则

- 页面外边距：移动端 `px-4 py-6`，桌面端逐步增加到 `lg:px-8`。
- 页面最大宽度：后台工作台默认 `max-w-7xl`。
- 页面区块间距：默认 `gap-4`。
- 卡片内部使用 shadcn `Card` 自带 spacing，不额外写死大段 padding。

### 列表页内部纵向节奏（高度与间距解耦）

> 行的内容高度 与 行间留白分开定义，不绑定。对齐公司 Figma 列表页。

- **页头行 / 工具栏行内容高度**：控件 28px（页头标题/按钮、工具栏筛选/搜索/图标均 28）。
- **内容卡外留白**：顶（顶栏↔卡）12、左（导航↔卡）12、右 12、**底 0**（`gap-3 pt-3 pr-3`，无 pb/pl）。
- **页头 ↔ 工具栏间距 = 8px**（页头 `pb-1` + 工具栏 `pt-1`，各 4）。
- **工具栏 ↔ 表头间距 = 8px**（工具栏 `pb-2`）。
- 页头顶部留白 `pt-3`(12)。
- **表格区左右内缩 12**（表格容器 `px-3`，行分割线/内容不顶卡片左右边；勾选/操作列只留小边距 `pl-1`/`pr-1`）。
- **底栏（分页）同 gutter**：`mx-3` 内缩 12，上分割线不顶卡片边。
- **表头 / 表格行高 = 36px**；进度条 60×4；搜索栏 290×28。
- 工具栏图标用 `Toggle`/`Button` 的方形 `toolbar-icon` 档（28×28、16px 图标）；带文字工具栏按钮用 `toolbar` 档（28px 高、14px 字），选中/hover 底块一致。

> 具体页面级模板（标准列表页结构、标题区/筛选区/表格区/弹窗的搭法）属于"页面级规范"，放进对应的具体页面文档，不在本基础布局规范里。
