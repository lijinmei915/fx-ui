node_modules
dist
.project-os/graph/
.DS_Store

# TS 增量编译缓存
*.tsbuildinfo

# 本地个人配置（不应跨人同步）
.claude/settings.local.json
*.local.*

# 环境变量（含密钥，不能提交）
.env
.env.local
.env.*.local

# Playwright 视觉回归：基线截图(tests/**-snapshots/)要提交，运行产物不提交
/test-results/
/playwright-report/
/blob-report/
